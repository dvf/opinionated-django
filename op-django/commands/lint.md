Run linting and static analysis on the codebase.

1. Run `uv run ruff check src` to lint the code.
2. Run `uv run ruff format --check src` to verify formatting.
3. Run `uv run pyrefly check src` for static type analysis.

Fix any issues found. Report a summary of results when done.
